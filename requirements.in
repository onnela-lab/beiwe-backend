# core libraries
Django>=5.2
Django<5.3
firebase-admin==5.4.0
Jinja2 # used to require pinning, I guess review if there is a major version bump.
orjson==3.9.15

mypy==1.15.0
django-stubs[1.15.0]
pyzstd

# django-extensions for some really nice management commands and terminal helpers.
django-extensions

python-dateutil

# testing libraries
# mock
# coverage
# tblib
# redgreenunittest
# time-machine

pyqrcode
pypng
pyotp

# This version of pycryptodome results in "from Cryptodome import ..." imports.
pycryptodomex==3.23.0

# error reporting
cronutils==0.4.2

# data sanitization
bleach>=4.1.0

# server requirement
psycopg[pool]

# ipython can exhibit weird issues that make it look like the codebase is broken.  Most issues
# are resolved by forcing this relatively new version of ipython.
ipython>=8.12.2
gunicorn

# Keep these dependencies up to date
boto3

# we actually had a requirements conflict _involving pip-tools_ so we are removing it from the requirements...
# pip-tools>=7.3.0


#! YOU MUST manually update the following line to the correct git hash. it must be 40 characters.
# current_forest_git_hash: f49b9898383e2a7a51302a5d45f8f2e573b2809e
# manual override prefix is git+https://git@github.com/onnela-lab/forest@haaash_goooeees_heeerreee
beiwe-forest==1.0

celery==5.4.0


rcssmin  # extremely fast css minifier
